<ECF>

    <!--
      GeLaCo bi-objective configuration — exactly the hyperparameters
      reported in arXiv:2507.10059 §4 ("Operational Framework"):

        Algorithm        : NSGA-II
        Population       : 200 individuals
        Termination      : 30,000 fitness evaluations
        Crossover        : integer-adapted SBX, p=0.9, eta_c=20.0
        Mutation         : polynomial, p=1/(3L)=1/96, eta_m=20
        Encoding         : 3L = 96 variables; b, e in [0, L-1], a in {0,1}
                           (the {0,1} projection is handled in the C++ decoder;
                           ECF FloatingPoint uses scalar bounds [0, L-1])
        Calibration      : 64 Wikipedia sentences (handled by server.py)

      The Python evaluator (server.py) is started by the PBS job script and
      connected over two named FIFOs (env vars GELACO_REQ_FIFO,
      GELACO_RSP_FIFO, GELACO_READY_FILE).
    -->

    <Algorithm>
        <NSGA2>
            <Entry key="popSize">200</Entry>
            <!-- Crossover and mutation probabilities controlled at the
                 genotype level below (NSGA-II calls into the genotype's
                 registered operators). -->
        </NSGA2>
    </Algorithm>

    <Genotype>
        <FloatingPoint>
            <Entry key="dimension">96</Entry>
            <Entry key="lbound">0.0</Entry>
            <Entry key="ubound">31.0</Entry>

            <!-- SBX crossover, integer-adapted via floor() in the decoder.
                 Paper §4: probability 0.9, distribution index 20.0. -->
            <Entry key="crx.sbx">0.9</Entry>
            <Entry key="crx.sbx.eta">20.0</Entry>

            <!-- Polynomial mutation. Paper §4: p = 1/(3L) = 1/96, eta = 20. -->
            <Entry key="mut.polynomial">0.010416667</Entry>
            <Entry key="mut.polynomial.eta">20.0</Entry>
            <!-- Disable other default mutation operators to match the paper. -->
            <Entry key="mut.simple">0.0</Entry>
        </FloatingPoint>
    </Genotype>

    <Registry>
        <!-- Reproducibility: match the Python data_loader seed. -->
        <Entry key="randomizer.seed">42</Entry>

        <!-- Termination on number of evaluations (paper's criterion). -->
        <Entry key="term.maxEvaluations">30000</Entry>

        <!-- Single run; bump if you want multiple independent seeds. -->
        <Entry key="batch.repeats">1</Entry>

        <!-- Logging: write the run to ecf.log and a Pareto archive. -->
        <Entry key="log.level">3</Entry>
        <Entry key="log.filename">ecf.log</Entry>
        <Entry key="log.frequency">1</Entry>

        <!-- Dump Pareto front + populations periodically. -->
        <Entry key="milestone.filename">milestone.txt</Entry>
        <Entry key="milestone.interval">500</Entry>
    </Registry>

</ECF>
